---
title: "String Functions"
description: "Functions for working with strings"
---

# String Functions

Functions for manipulating string data.

## Common Functions

| Function | Description |
|----------|-------------|
| `concat(s1, s2, ...)` | Concatenate strings |
| `lower(s)` | Convert to lowercase |
| `upper(s)` | Convert to uppercase |
| `trim(s)` | Remove whitespace |
| `substring(s, start, length)` | Extract portion |
| `length(s)` | String length |

## Pattern Matching

| Function | Description |
|----------|-------------|
| `like` | Pattern matching |
| `regexp_like(s, pattern)` | Regex matching |
| `regexp_extract(s, pattern)` | Extract regex match |

---

**Status:** 🚧 Stub — Content pending
**Owner:** [TBD]
**Priority:** Low

